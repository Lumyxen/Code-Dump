; This file is loaded from the same directory as the compiled binary.

[settings]
filter_mode = blacklist
working_directory = ../

[blacklist]
files = dump.md, .gitignore
directories = .git, .vscode, .gradle, .idea, scripts, dist, test, build, gradle

[whitelist]
files = Makefile
directories = src
